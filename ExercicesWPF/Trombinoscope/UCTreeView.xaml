<UserControl x:Class="Trombinoscope.UCTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Trombinoscope"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <TreeView ItemsSource="{Binding}">
        <TreeView.Resources>
            <HierarchicalDataTemplate DataType="{x:Type local:Employé}" ItemsSource="{Binding}">
                <TextBlock Text="{Binding Tag}"/>
            </HierarchicalDataTemplate>

            <DataTemplate DataType="{x:Type local:Territoire}">
                <TextBlock Text="{Binding Tag}"/>
            </DataTemplate>
        </TreeView.Resources>
    </TreeView>
</UserControl>
